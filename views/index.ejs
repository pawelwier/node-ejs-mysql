<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <% for (let x of userScoreArray) {%>
            <div>
        <%= `User: ${x.username}, Score: ${x.score}\n` %>
        <% } %>
            </div>
    </div>
    Nowy wynik:
    <form action='/change' method="POST">
        <select name="nameChangeScore">
            <% for (let x of userScoreArray) {%>
                <option>
            <%= `${x.user_id}. ${x.username}` %>
            <% } %>
                </option>
        </select>
        <input type='text' name='scoreOverwrite'>
        <input type='submit'>
    </form>
    <br>
    <!-- Dodaj wynik:
    <form action='/change' method="POST">
        <input type='text' name='addHundredScore'>
        <input type='submit'>
    </form> -->
</body>
</html>